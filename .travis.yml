language: java
jdk:
 - oraclejdk8
before_install:
 - chmod +x mvnw
deploy:
  provider: azure_web_apps
  username: meerkat       # If AZURE_WA_USERNAME isn't set
  password: Elec5620   # If AZURE_WA_PASSWORD isn't set
  site: elec5620       # If AZURE_WA_SITE isn't set
  slot: elec5620      # (optional) If AZURE_WA_SLOT isn't set
